Basic pug-based template for various open-source project websites of CG Internals.

Commands for building the website to `_site` folder for deployment/distribution:
```
npm install
npm run-script build
```
